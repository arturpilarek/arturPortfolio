{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "deploy-fb": "cd .output/server && rm -rf /s /q node_modules && npm i && cd .. && firebase deploy",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/content": "^2.4.3",
    "@nuxtjs/tailwindcss": "^6.3.0",
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.0",
    "firebase-functions-test": "^3.0.0",
    "nuxt": "^3.1.0",
    "nuxt-icon": "^0.2.10"
  },
  "dependencies": {
    "@morev/vue-transitions": "^2.3.6",
    "aos": "^2.3.4"
  }
}
